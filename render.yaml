services:
  - type: web
    name: duo-life-sms-webhook
    runtime: node
    rootDir: webhook-server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: FIREBASE_SERVICE_ACCOUNT_JSON_B64
        sync: false
