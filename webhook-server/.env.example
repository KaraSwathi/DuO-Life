WEBHOOK_TOKEN=replace-with-random-secret
PORT=8787
# Option A: local/service file path
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/serviceAccountKey.json

# Option B: full JSON string (escaped) for cloud env vars
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
